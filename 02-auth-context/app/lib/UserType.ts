export type LoginType = {
  email: string;
  password: string;
};

export type RegisterType = {
  email: string;
  name: string;
  password: string;
};
export type User = {
  email: string;
  name: string;
  password: string;
};
